# Shellforge

Shellforge is the custom shell module of the ForgeOS project.

Use `src/` for implementation files, `include/` for headers, and `docs/` for design notes, architecture notes, and progress reports.
